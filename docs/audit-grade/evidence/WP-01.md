# WP-01 - Evidencia de CI/CD protegido

Estado: `IMPLEMENTED_PENDING_MANUAL_GITHUB_GATES_AND_INDEPENDENT_REVIEW`

Issue: [#36](https://github.com/MauroTravisany/firstlayer_stock_market/issues/36)

PR draft: [#51](https://github.com/MauroTravisany/firstlayer_stock_market/pull/51)

Rama: `agent/wp-01-protect-main-ci-deploy`

Base WP-00: `3a47e12d57848f30bc9ffa67a5e34f4fe5853d71`

Fecha de verificacion: `2026-08-10`

No se desplego, no se ejecuto Terraform apply, no se modifico GCP, Dataform production, schedulers, servicios ni secretos. `Deploy Cloud Run service` permanecio `disabled_manually`. Strategy Brain permanece `PAUSED`, `SHADOW_ONLY` y `BACKTEST_ONLY`; Alpaca permanece Paper.

## Arquitectura resultante

```text
PR o push a main
  -> checks ligeros: workflows, action pinning, invariantes, secretos y unit tests
  -> preflight gate
  -> builds inmutables + Dataform/dashboard/Terraform validation
  -> CI gate del SHA exacto
  -> workflow_run exitoso de un push a main
  -> verificar repo, branch, evento, conclusion y SHA actual de main
  -> environment production y aprobacion independiente
  -> repetir verificacion del SHA aprobado
  -> publicar las ocho imagenes verificadas y resolver digests
  -> sincronizar exactamente APPROVED_SHA:dataform
  -> compilar Dataform sin errores, sin promover aun
  -> capturar release Dataform y trafico Cloud Run completos
  -> staged revision sin trafico -> metadata -> GET /readyz
  -> promover trafico -> GET /healthz
  -> promover el compilation result validado de Dataform
  -> guardar evidencia o restaurar release y trafico exactos
```

`.github/workflows/ci.yml` se ejecuta para `pull_request` hacia `main`, `push` a `main` y `workflow_dispatch`. Sus jobs usan permisos read-only. Los Docker builds dependen de `preflight-gate`, por lo que no comienzan antes de validar workflows, invariantes, secretos y tests.

`.github/workflows/deploy.yml` no acepta `pull_request`, `push`, `master` ni `workflow_dispatch`. Solo consume un `workflow_run` exitoso de `CI` cuyo evento original fue un push a `main`, desde el mismo repositorio, y cuyo `head_sha` sigue siendo el SHA remoto actual de `main`. El deploy permanece deshabilitado y no fue ejecutado.

`deploy-dashboard.yml` aplica el mismo contrato para `workflow_run`: rechaza otro repositorio, CI fallido, evento distinto de push y SHA obsoleto. Repite la consulta del SHA remoto inmediatamente antes de `actions/deploy-pages`.

## Invariantes de seguridad

`verify_repo_invariants.py` falla cerrado ante `PAPER_CHAMPION`, modos LIVE, ejecución real, Strategy Brain fuera de `BACKTEST_ONLY`, `production_change_allowed=true`, schedulers Strategy Brain reactivados en el baseline, deploy desde `master`/PR/push directo/dispatch, secretos de alta confianza, eliminación del lock legacy o resultados `LEGACY_PRE_AUDIT_GRADE` elegibles para promoción. La ejecución real produjo `REPO_INVARIANTS_VALID` y las pruebas negativas exigen exit code no cero.

## Build once e identidad

CI construye ocho imagenes desde `scripts/ci/cloud_run_services.json`. Cada artifact conserva SHA Git, image ID, `image.tar.gz` y checksum. Deploy descarga artifacts del run exacto, comprueba checksum/SHA, carga la imagen y compara el image ID real de Docker con `metadata.json`; metadata manipulada falla. Publica una vez y despliega exclusivamente por digest `@sha256:`.

Todas las GitHub Actions estan fijadas a commits SHA completos. Actionlint esta fijado a un digest Docker `sha256`. `verify_action_pinning.py` y sus tests rechazan tags y referencias mutables.

## Promocion Dataform

Despues del approval y de volver a validar el SHA de `main`, el workflow crea un commit de snapshot cuyo tree es exactamente `APPROVED_SHA:dataform`, lo empuja a `dataform-production` y confirma el SHA remoto. Exige que el release `production` no tenga una programacion automatica activa para evitar una carrera mientras se valida.

El compilation result debe resolver al commit de snapshot, pertenecer al release esperado y contener cero `compilationErrors`. El release no cambia hasta que todas las revisiones Cloud Run pasan staged smoke y post-promotion smoke. La evidencia registra git SHA, dataform tree SHA, snapshot commit, compilation ID, release anterior, release nuevo y rollback metadata. Ante fallo, restaura el `releaseCompilationResult` anterior.

`dataform_promotion.py` tiene pruebas negativas para compilation errors, SHA incorrecto, release apuntando a otra compilacion y rollback metadata ausente. Esta promocion no se ejecuto contra Dataform production durante el PR.

## Smoke funcional no mutante

`smoke_test_services.py` conserva como primera capa `gcloud run revisions describe`: exige `Ready=True`, generacion observada e imagen igual al digest aprobado. Como segunda capa obtiene un identity token con audience explicita para servicios privados y ejecuta solo `GET /readyz` sobre la revision staged y `GET /healthz` despues de promover trafico.

Cada servicio expone ambos probes antes de cargar configuracion de negocio. La respuesta incluye `status`, `service`, `git_sha`, `version`, `image_digest`, `operation=readiness_probe` y `mutation_performed=false`. El cliente limita timeout y tamano, y falla ante HTTP no 200, timeout, JSON malformado, SHA/digest incorrecto o campos que indiquen orden, estrategia, escritura, alerta o mutacion. POST sobre esos endpoints devuelve 405.

Las pruebas cubren HTTP 200, HTTP 500, timeout, SHA/digest incorrectos, respuesta malformada, intento de operacion prohibida y autenticacion privada. Los ocho contenedores que incluyen los probes compilaron localmente. No se llamo ningun servicio live.

## Rollback exacto

Antes de crear revisiones, `cloud_run_traffic.py` captura `spec.traffic`, `status.traffic`, revisiones, porcentajes, tags, `latestReadyRevision` y `latestCreatedRevision` de cada servicio. El artifact se conserva aun cuando falle el workflow.

El rollback opera en orden inverso, limpia tags transitorios, restaura el mapa de revisiones/porcentajes y repone todos los tags anteriores. No sustituye la revision activa por `latestReadyRevision`. Es idempotente cuando el estado ya coincide y agrega errores si algun servicio no puede restaurarse.

Si la promocion termina pero GitHub no puede retener el artifact de evidencia, un paso protegido por `failure()` restaura tambien el release Dataform y todo el trafico previo; una promocion sin evidencia auditable no queda activa.

Las pruebas cubren 100/0, 90/10, multiples tags, latestReady distinto de la revision activa, fallo tras promover varios servicios, fallo durante rollback e idempotencia. Falta un drill live autorizado, por lo que OP-05 permanece `PARTIAL`.

## Dependencias

CI ejecuta `npm audit --json` para dashboard y Dataform y aplica `npm_audit_gate.py` con umbral critical fail-closed. No existe excepcion global por severidad. La allowlist versionada identifica cada advisory autorizado por GHSA/CVE, paquete, version, superficie, razon, owner, issue de remediacion y expiracion; falla ante advisory nuevo, excepcion expirada/incompleta o cambio de paquete/version.

Dataform conserva deuda critica heredada de `vm2@3.9.19`: 20 registros exactos autorizados temporalmente hasta `2026-09-30`, con seguimiento en Issue #36. Dashboard no tiene findings critical autorizados. Los `npm audit` crudos devuelven exit 1 por deuda no critica y/o autorizada; el gate controlado devuelve 0 solo cuando no existe critical no autorizado.

Dependabot cubre GitHub Actions, npm de dashboard/Dataform, Terraform y pip `/` para `requirements-ci.txt`.

## Estado remoto y gates manuales

Ultima evidencia read-only previa, conservada sin afirmar cambios durante esta correccion:

```text
default branch: master
rulesets: master_only (active)
environment production: ausente
Deploy Cloud Run service: disabled_manually
strategy-brain-generate: PAUSED
strategy-brain-review: PAUSED
```

Siguen pendientes y deben mantenerse separados:

1. cambiar la default branch de `master` a `main`;
2. crear `protect-main`, exigir PR, required checks, una revision independiente, CODEOWNERS, conversaciones resueltas y bloquear force-push/eliminacion;
3. crear el environment `production`, limitarlo a `main` y exigir reviewer independiente;
4. hacer la transicion y retiro controlado del workflow legacy del dashboard;
5. reactivar explicitamente `Deploy Cloud Run service` solo despues de fusionar, verificar los gates y obtener autorizacion separada.

El procedimiento exacto esta en [WP-01_CI_CD_GOVERNANCE.md](../runbooks/WP-01_CI_CD_GOVERNANCE.md). Ningun gate manual se declara aplicado.

## Verificacion ejecutada

### Gap demostrado antes de implementar

| Comando | Exit | Evidencia |
|---|---:|---|
| Tests nuevos de SHA obsoleto, Dataform, smoke, rollback, npm audit, pinning e image ID | 1 | Modulos/contratos ausentes o comportamiento insuficiente. |

### Verificacion local final

| Comando | Exit | Evidencia |
|---|---:|---|
| `python -m unittest discover -s tests -p "test_*.py"` | 0 | 105 tests. |
| `python -m compileall -q tools scripts tests cloud-functions` | 0 | Python completo. |
| `python scripts/ci/verify_repo_invariants.py` | 0 | `REPO_INVARIANTS_VALID`. |
| `python scripts/ci/verify_action_pinning.py` | 0 | Todas las actions y actionlint inmutables. |
| actionlint por digest inmutable | 0 | Workflows validos. |
| `npm ci` + `dataform compile` | 0 | 219 acciones compiladas. |
| dashboard `sources:strict` + `build:strict` en Node 20 Linux limpio | 0 | Fuentes y sitio Evidence construidos. |
| dashboard `npm audit --json` | 1 | Deuda moderate/high; cero critical. |
| dashboard `npm_audit_gate.py` | 0 | Ningun critical no autorizado. |
| Dataform `npm audit --json` | 1 | Deuda heredada, incluidos findings exactos de vm2. |
| Dataform `npm_audit_gate.py` | 0 | Solo excepciones exactas y no expiradas. |
| Terraform 1.9.8 `fmt -check`, `init -backend=false`, `validate` | 0 | Tres controles reproducibles en Docker. |
| builds Docker de los ocho servicios | 0 | Ocho `BUILD_PASS`; ninguna imagen publicada. |
| `git diff --check` | 0 | Sin errores de whitespace. |

Los comandos finales se repiten despues de cerrar la documentacion y antes de publicar el commit. El run remoto del PR se agregara como evidencia del mismo SHA; un resultado pendiente o fallido no se presentara como PASS.

## Criterios de aceptacion

| Criterio | Estado | Evidencia |
|---|---|---|
| Un PR no puede desplegar | PASS | Trigger y tests de contrato. |
| Un push a `master` no puede desplegar | PASS | `master` ausente de triggers productivos; invariant test. |
| Solo `main` puede iniciar deploy | PASS | `workflow_run`, origen/evento y SHA actual verificados. |
| CI fallido bloquea deploy | PASS | Conclusion success obligatoria y tests negativos. |
| SHA e imagen desplegados coinciden con lo aprobado | PASS | Checkout exacto, checksum, image ID y digest inmutable. |
| Dataform usa el SHA aprobado y rollback metadata | PASS | Implementacion y tests de contrato; promocion live no ejecutada. |
| Approval gate existe en codigo | PASS | `environment: production` precede autenticacion/promocion. |
| Approval gate esta configurado en GitHub | PARTIAL | Pendiente manual; ultima lectura indico environment ausente. |
| Invariant checker bloquea estados peligrosos | PASS | Checker real y pruebas positivas/negativas. |
| Smoke funcional no mutante definido y probado | PASS | Dos capas, probes en ocho servicios y pruebas negativas. |
| Rollback restaura trafico exacto en fixtures | PASS | Casos 100/0, 90/10, tags, fallos e idempotencia. |
| Rollback live verificado | PARTIAL | No autorizado ni ejecutado. |
| Dashboard rechaza SHA obsoleto | PASS | Doble check y tests A/B, repo, CI y evento. |
| Critical dependency gate fail-closed | PASS | Audits, allowlist exacta y tests de expiracion/cambio. |
| Actions y actionlint fijados inmutablemente | PASS | Verificador y actionlint. |
| Default branch y proteccion de `main` aplicadas | PARTIAL | Pasos remotos pendientes. |
| Transicion del dashboard legacy aplicada | PARTIAL | Procedimiento documentado; ejecucion remota pendiente. |
| No hubo deploy ni mutacion live | PASS | Solo codigo y pruebas locales; workflow sigue deshabilitado. |

## Riesgos residuales

- default branch, `protect-main`, required checks y environment `production` aun no estan configurados;
- falta reviewer independiente y revision solicitada de este PR;
- workflow Cloud Run permanece intencionalmente deshabilitado;
- promocion/rollback Dataform y rollback Cloud Run no tienen drill live;
- el release Dataform debe permanecer sin schedule automatico durante la promocion; el workflow falla cerrado si detecta uno;
- WIF pertenece a WP-10; deploy conserva `GCP_SA_KEY` solo despues del approval;
- excepciones criticas temporales de `vm2@3.9.19` expiran el `2026-09-30`;
- la transicion del workflow legacy del dashboard sigue pendiente.

## Archivos de esta correccion

- `.github/dependabot.yml`
- `.github/npm-audit-allowlist.json`
- `.github/workflows/ci.yml`
- `.github/workflows/deploy-dashboard.yml`
- `.github/workflows/deploy.yml`
- `cloud-functions/*/main.py` de los ocho servicios
- `cloud-functions/*/runtime_health.py` de los ocho servicios
- `docs/audit-grade/08_traceability_matrix.md`
- `docs/audit-grade/evidence/WP-01.md`
- `docs/audit-grade/runbooks/WP-01_CI_CD_GOVERNANCE.md`
- `scripts/ci/cloud_run_traffic.py`
- `scripts/ci/dataform_promotion.py`
- `scripts/ci/npm_audit_gate.py`
- `scripts/ci/smoke_test_services.py`
- `scripts/ci/verify_action_pinning.py`
- `scripts/ci/verify_main_sha.py`
- `scripts/ci/verify_release_artifacts.py`
- `tests/unit/ci/test_action_pinning.py`
- `tests/unit/ci/test_cloud_run_traffic.py`
- `tests/unit/ci/test_dataform_promotion.py`
- `tests/unit/ci/test_npm_audit_gate.py`
- `tests/unit/ci/test_repository_controls.py`
- `tests/unit/ci/test_runtime_health_contract.py`
- `tests/unit/ci/test_smoke_test_services.py`
- `tests/unit/ci/test_verify_main_sha.py`
- `tests/unit/ci/test_verify_release_artifacts.py`
- `tests/unit/ci/test_workflow_contracts.py`

Se solicita una nueva revision independiente antes de cualquier merge, gate remoto o reactivacion.
